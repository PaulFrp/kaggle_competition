## Setup

```bash
git clone <https://github.com/PaulFrp/kaggle_competition>
cd <repo-folder>

python3 -m venv .venv
source .venv/bin/activate  # on Windows: .venv\Scripts\activate

pip install -r requirements.txt